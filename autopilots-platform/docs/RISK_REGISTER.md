# Risk Register

| Risk | Impact | Current control | Owner / next action |
| --- | --- | --- | --- |
| Demo mistaken for production | Critical | Persistent demo labels; no external writes | Product: retain environment badges |
| Cross-tenant access | Critical | Server organization checks plus PostgreSQL RLS in the local foundation | Engineering: execute adversarial RLS tests against a disposable Supabase sandbox |
| Credential leakage | Critical | Raw demo secrets discarded | Security: managed vault and rotation |
| Incorrect financial truth | High | Demo cost quality label; no margin claims | FinOps: provider reconciliation |
| Duplicate/replayed effects | High | Tenant-scoped durable idempotency contract in the local foundation | Platform: validate concurrent requests in sandbox |
| Stale human approval | High | Context version and R3 approval are created transactionally | Platform: add execution-time row locking before enabling commands |
| In-memory product-state loss | High | Identity sessions are now durable; demo product workflows remain explicitly in memory | Platform: migrate portfolio reads and governed commands after sandbox acceptance |
| Provider outage/rate limit | High | Bounded read-only probes, stable safe error codes and explicit degraded/unavailable states | Integrations: add timeout budgets, backoff and alert deduplication before continuous polling |
| Overbroad agent autonomy | High | Server allowlist, risk policy, kill switch | AI Ops: evals and earned autonomy |
| Legal evidence incomplete | High | Version/hash fixture only | Legal: approve documents/retention |
| Operating-brand metrics mistaken for live | Critical | Demo environment, source quality and unavailable values shown explicitly | Data: require successful sync and reconciliation before promotion |
| Brand/customer identifier collision | Critical | Immutable prefixed IDs and server membership checks | Data: preserve IDs in PostgreSQL migration |
| Missing AutoReviews mappings | High | Four external sources fail closed as blocked | Integrations: configure sandbox IDs and reconciliation |
| Control plane becomes a product-data monolith | High | Product-owned data planes and a versioned snapshot contract | Architecture: reject raw operational-table replication |
| AI links the wrong external resource | Critical | AI mappings remain proposals with confidence and evidence | Operator must approve mappings before promotion |
| Migration targets the wrong Supabase project | Critical | The runner accepts only project `wurycoodzcybaxcgqxps`, derives it from direct hosts or pooler usernames before connecting, and still requires explicit permission/change ID; CI verifies immutable applied checksums | Operations: keep automated deployment absent until backup/restore and environment promotion are separately authorized |
| Legacy `public` tables lack RLS | Critical | All 18 legacy tables now have RLS and no browser grants; the active insert/upsert contracts passed under service role with zero rollback residue; Advisor errors are 0 and live counts remain 17/3/59/59 | Engineering: retain the live verifier and re-run the rollback contract before changing any mirror schema, conflict key or authority |
| Legacy trigger resolves mutable search path | High | `public.set_updated_at()` now has fixed `pg_catalog, public` resolution; a temporary trigger acceptance proved behavior and rollback residue zero while four dependent Gift triggers stayed intact | Security: keep configuration immutable and re-run dependency inspection before changing the function body |
| Gift schema exposes sensitive object names through GraphQL | Critical | All six RLS tables now deny browser privileges and live REST probes return 401; service-role counts remain 2/2/5/10/1/0 and no data was changed | Legacy Gift owner: identify any external service-role application before changing records, schema, retention or server authority |
| Supabase token leaks into application storage or logs | Critical | Provider tokens exist only during the MFA callback; the application stores only a SHA-256 hash of its own 256-bit cookie | Security: add automated log-redaction tests before production |
| Owner operates at AAL1 before MFA setup | Critical | The managed callback creates no Autopilots session until TOTP enrollment or challenge returns an AAL2 token | Identity: owner completes enrollment from the delivered login link; add a second recovery factor later |
| Stale access after role or membership revocation | Critical | Every cookie resolution rejoins active profile and membership state; logout writes revocation and audit evidence | Identity: add live suspension/revocation tests with a disposable second user |
| Health endpoint exposes upstream internals | High | Adapter returns a versioned allowlisted contract and strips private upstream error details | Security: retain response-bound and disclosure tests for each new connector |
| A reachable endpoint is mistaken for production readiness | Critical | UI distinguishes healthy, degraded and unavailable and labels every probe `live_readonly_probe`; provider writes remain false | Operations: require dependency, reconciliation and human promotion gates before production status |
| Polling creates duplicate alert noise | High | Observation keys are idempotent and one partial unique index deduplicates each active brand/connector/error incident | Operations: add bounded polling cadence and notification suppression windows |
| Operator acknowledges stale incident context | High | R1 acknowledgement locks the incident and requires the current occurrence count as context version | Runtime: surface refresh-required errors and never auto-retry an acknowledgement with changed context |
| Business conflict is mistaken for retryable database failure | High | Stale incident context uses explicit `P0001`; runtime maps the exact message to HTTP 409 and acceptance verifies bounded response time | Engineering: reserve and document application SQLSTATE/message pairs before adding new commands |
| Health page view silently mutates evidence | High | All GET health and incident routes are side-effect free; persistence is an explicit JSON POST with an idempotency key and MFA | Security: retain route-order and no-write acceptance checks |
| Multiple runtime instances duplicate scheduled probes | High | A unique database lease per scheduler key/time bucket elects one holder; heartbeat, bounded expiry recovery and observation idempotency add independent controls | Platform: add multi-instance load and lease-expiry chaos acceptance before production hosting |
| Monitoring silently becomes stale after a runtime outage | High | Service-role freshness snapshot labels every brand fresh, stale or never observed against a bounded threshold | Operations: add notification suppression and escalation delivery after the provider channel is explicitly authorized |
| Scheduler impersonates a human owner | Critical | Monitoring v2 requires the non-login service principal `autopilots:health-monitor`; the owner profile is rejected by the machine RPC and legacy human lease RPCs are revoked | IAM: move the server transport credential into the approved production vault before hosting |
| Machine identity gains broad platform authority | Critical | Service-principal scopes use an allowlist, legal-entity/brand foreign keys, active status and hard database checks that login and external writes remain false | Security: add separate principals per future workload and never reuse the monitor for provider activation |
| Access request accidentally becomes a real invitation | Critical | Staging stores an R2 command and pending approval only; database checks force provider invitation and external-write flags false | IAM: require separate explicit authority and a new reviewed executor before any invitation transport exists |
| Connector request accidentally starts provider access | Critical | Connector staging is an internal R2/R3 intent only; five database constraints force authorization, account connection, discovery, credential storage and external writes to remain false, while the UI and RPC expose no provider executor | Integrations: require separate explicit provider permission, current human approval and a new reviewed executor before any OAuth or discovery route exists |
| Approved connector intent is mistaken for an active connection | Critical | Approval closes the locked R2/R3 request and its pending command but returns five explicit no-effect flags; the decision command, UI and audit reason all state that provider execution remains unapplied | Product: display approved intent separately from connection health and require a new authorized executor before activation |
| Privilege escalation through an owner grant | Critical | The staged role allowlist excludes `owner`; only legal-entity-wide owner/admin memberships may stage other roles | IAM: define a break-glass, dual-control owner-transfer procedure before implementing owner changes |
| Stale or replayed access decision | Critical | Approval/rejection locks the request, requires its current context version, creates a separate idempotent R2 decision command and rejects divergent reuse | Identity: retain the same context lock when a separately authorized membership apply step is designed |
| Approved intent mistaken for active access | Critical | Decision evidence explicitly records `membershipApplied=false`, `providerInviteSent=false` and `externalWrites=false`; the roster keeps status separate from active memberships | Product: keep approved requests visibly blocked until a separately authorized apply workflow exists |
| Deployed migration history is silently rewritten | Critical | The platform gate pins all 32 applied filenames and SHA-256 checksums, requires transaction wrappers and rejects unregistered files | Database: correct defects only through a separately reviewed immutable migration |
| Repository credential is committed | Critical | The platform gate scans every tracked platform file for live Stripe/Supabase keys, assigned service-role credentials and private-key blocks | Security: expand allowlisted detectors with each new provider and use a managed vault before hosting |
| Connector configuration exfiltrates a secret or probes an internal service | Critical | Remote destinations require HTTPS and an exact separate allowed origin; credentials/query/fragment URLs fail before fetch; loopback HTTP is local-only and responses are bounded | Integrations: add DNS/IP resolution pinning at the production egress proxy before remote hosting |
| Portfolio fixture is mistaken for current company truth | Critical | Managed `/api/v1/os/portfolio` reads service-role-only `autopilots.portfolio.v2`; launch progress is derived from governed steps/requests, missing customers/goals remain explicit zero and finance stays unavailable; `demoMode=false` | Data: add each future business projection only with a named authority, freshness and reconciliation contract |
| Launch percentage is mistaken for production readiness | Critical | Portfolio v2 labels only governed onboarding-step completion, shows attention/approvals/next action, keeps provider authorization and writes false, and does not expose a production-ready flag | Product: retain separate restore, identity, provider and deployment gates before any production claim |
| New software request is mistaken for an existing operating brand | Critical | Staging is an R2 intent under the existing Autopilots authority; six database constraints keep brand, sandbox, onboarding, provider authorization, credentials and writes absent, and live request count is zero | Platform: require a separate R3 apply design and explicit owner acceptance before creating any brand or environment |
| Approved software launch is mistaken for an applied brand | Critical | Approval closes only the current R2 intent; immutable false constraints, result evidence and the absence of an apply function keep brand, sandbox, onboarding, provider authorization, credentials and writes absent | Product: label approved as internal intent and require a separate R3 creation workflow with recovery evidence |
| Demo approval is mistaken for live workflow authority | Critical | Managed Approval Center now fails closed without the service-role-only organization queue; it exposes bounded evidence but no command payload, generic decision route or provider action | Product: add typed deep links only after each target workflow has current-context acceptance coverage |
| Demo task or unbounded error detail is mistaken for live operations truth | Critical | Managed Human Task Inbox fails closed without the service-role-only operations queue; signals omit incident context, command payload/result and expose no generic task action or automatic remediation | Operations: add typed runbook links and suppression only through separately reviewed, source-specific workflows |
| Brand Digital Twin mixes fixtures with governed operational truth | Critical | Managed brand detail now reads `autopilots.brand-twin.v1`; membership, connectors, health, incidents and ledger costs come from Supabase while unsupported projections stay empty/null | Data: migrate each new projection independently and reject fixture fallback in managed mode |
| Managed action appears successful but exists only in process memory | Critical | Supabase mode rejects `/api/v1/demo/command` with `MANAGED_COMMAND_ROUTE_REQUIRED`; the client also blocks it locally; monitoring, incident and IAM actions use dedicated durable contracts | Platform: migrate each remaining useful action as its own risk-classified command before exposing it in managed UI |
| Backup exists but cannot be restored within required RPO/RTO | Critical | Read-only verifier requires a fresh completed physical backup, WAL-G and expected region, but hard-codes `restoreRehearsed=false`; seven daily backups are currently listed and PITR is off | Operations: obtain approval and rehearse into a disposable project; record RPO/RTO and destroy the copy |
| New database RPC accidentally becomes browser callable | Critical | Release validation discovers every `public.autopilots_*` function and requires an exact reviewed caller role; only the signed-in session-context RPC permits `authenticated`, all others deny browser roles | Security: keep the matrix exact and review grants with every migration |
| Negative scope checks pass but two valid memberships still cross-read | Critical | Eleven recurring wrong-scope/anonymous checks pass and the rollback second-tenant matrix now verifies 5 denials, 4 scoped contracts and zero residue; it exposed and drove the incident-v2 correction | Security: re-run the matrix whenever a portfolio RPC or tenant model changes |
| Superseded monitoring v1 RPC remains callable by a compromised server credential | High | Runtime already uses service-principal v2; a forward migration revokes v1 execution without dropping functions or data and the access matrix marks all four endpoints disabled | Platform: verify live denial and retain v2 scheduler acceptance after migration |
| Multi-organization profile mixes incidents across legal entities | Critical | Rollback probe reproduced the leak in v1; v2 requires legal-entity ID, membership and per-row organization filtering, v1 is revoked, and the full matrix passed with zero residue | Security: retain this matrix as a required regression whenever portfolio scope changes |
| Authenticated browser can discover governed OS tables through GraphQL | High | Session-context v2 explicitly scopes one legal entity; authenticated direct table SELECT and helper execution are revoked, leaving one bounded login RPC; the Advisor rerun now shows zero findings in the six governed schemas | Security: retain exact-surface tests and complete the real-owner TOTP login acceptance |
| Managed internal screen presents session-demo state as durable truth | Critical | Internal Overview routes to the Supabase Brand Digital Twin, Implementatie routes to governed onboarding evidence, and Agent Control Room reads the durable registry or fails closed; route-boundary tests prevent demo fallthrough | Product: complete visual acceptance after owner TOTP and approve the first authoritative agent observation source |
| Registered agent is mistaken for a live or controllable runtime | Critical | The registry separates lifecycle from runtime, requires timestamped observed/reconciled evidence for every non-unknown runtime status, starts empty and constrains controls and external writes false | AI Ops: approve one authoritative observation source before inserting the first agent; design controls separately |
