# AP-OPS-20260803-008 — Organization monitoring history

- Applied: 2026-08-03
- Project: Autopilots (`wurycoodzcybaxcgqxps`)
- Migration: `20260804085000_organization_monitoring_history.sql`
- SHA-256: `eb309784dfc58af707d79be815920d1451084b1ec4909efbd1fb52e0002a88cf`
- Change ID: `AP-OPS-20260803-008`
- External writes: disabled

## Boundary

This change adds one stable organization-scoped read contract over existing
`integration.monitoring_runs`. Active legal-entity-wide owners, admins,
operators and auditors may read at most the latest 20 runs through the server.
The contract exposes safe status, counts, error code, timing and lease-recovery
evidence. Runtime holder IDs and human/machine profile IDs are omitted.

The function cannot create or update a monitoring run. Automatic remediation,
notification delivery and external writes are explicitly false. Browser roles
cannot execute the function; the application server rechecks the durable
profile and organization membership.

## Live acceptance

The transaction and immutable registry insert succeeded on the exact Autopilots
project. Service-role REST returned `autopilots.monitoring-history.v1` with 15
real recent runs. The initial summary counted scheduler outcomes only; the
separate product-attention semantics were corrected through immutable forward
change `AP-OPS-20260803-009`. An unknown profile was denied with SQLSTATE
`42501`; anonymous execution returned 401.

The freshly rerun Supabase Security Advisor reports 0 errors, 2 known warnings
and 33 information items. After its forward correction, the complete platform
gate passes 233 tests, 34 immutable migration checksums and 32 governed public
RPCs.

## Recovery

If the projection causes an incident, stop the local managed runtime and remove
the panel in a forward application change. Do not edit the applied migration.
The function is read-only and changed no monitoring records, provider account
or product-owned data.
