# AP-OPS-20260803-005 — Retire monitoring v1 RPC execution

- Applied: 2026-08-03
- Project: Autopilots (`wurycoodzcybaxcgqxps`)
- Migration: `20260804013000_retire_monitoring_v1_rpcs.sql`
- SHA-256: `19c52ec16453b1372ebce0ca5fb156c63c6a0bc31560d07c8648aabedc3c32da`
- Change ID: `AP-OPS-20260803-005`
- External/provider writes: disabled

## Boundary

The scheduler has used the allowlisted `autopilots:health-monitor` service
principal and v2 monitoring lease contracts since AP-OPS-20260803-004. The
four earlier human-authority lease functions were no longer referenced by the
runtime but remained executable by `service_role`.

This forward migration revokes only their execute grants. It does not drop a
function, delete data or alter monitoring evidence. The access matrix keeps the
function names visible as deliberately disabled so a later grant cannot be
introduced without breaking review expectations.

## Live acceptance

| Check | Result |
| --- | --- |
| v1 claim execute | false |
| v1 heartbeat execute | false |
| v1 complete execute | false |
| v1 freshness execute | false |
| v2 service-principal claim execute | true |
| Registry checksum/change ID | exact |
| Local platform checks | 134/134 passed |

The active local runtime remained healthy with durable monitoring enabled and
`externalWrites=false` after the migration.
