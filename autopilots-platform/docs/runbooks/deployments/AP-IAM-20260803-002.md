# AP-IAM-20260803-002 — Authenticated session context

- Applied: 2026-08-03
- Project: Autopilots (`wurycoodzcybaxcgqxps`)
- Migration: `20260803174500_authenticated_session_context.sql`
- SHA-256: `a8ca71efe5ef3b5c58cc549064aa72401bc73c7e68059b5c0dc86ada7f0a656c`
- Callback added: `http://127.0.0.1:4310/auth/callback`
- Existing Site URL changed: no
- Provider writes: disabled

## Evidence

`public.autopilots_session_context()` is a stable `security invoker` function.
It uses `auth.uid()`, active profile and membership state, and legal-entity or
brand scope to return only the current user's display identity, IAM role, MFA
requirement, JWT assurance level and accessible brands.

Live privilege verification returned:

| Check | Result |
| --- | --- |
| Security definer | false |
| Anonymous execute | false |
| Authenticated execute | true |
| Migration checksum recorded | true |

The sandbox runtime verifies Supabase tokens server-side and runs TOTP
enroll/challenge/verify during the callback. Temporary access and refresh tokens
remain only in that tab's JavaScript memory and per-request server memory. Only
after Supabase returns an `aal2` token does the server create a hashed opaque
HttpOnly Autopilots session handle.
