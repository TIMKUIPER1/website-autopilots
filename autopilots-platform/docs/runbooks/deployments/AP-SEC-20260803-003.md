# AP-SEC-20260803-003 — Protect active legacy mirrors

- Activated: 2026-08-03
- Migration: `20260804033000_protect_active_legacy_mirrors.sql`
- Supabase project: `wurycoodzcybaxcgqxps`
- Change ID: `AP-SEC-20260803-003`
- Checksum: `0259adb16fed695b7d0150451d061b1f4d8e0d2cfa76cb8aa1704a2cbfd38b99`
- Provider, identity, membership and external writes: not applied

## Boundary

The migration enables RLS and revokes `public`, `anon` and `authenticated`
privileges on `audit_log`, `integration_health`, `invoices_sales` and
`raw_imports`. It changes no records, policy, column, index, constraint or
service-role grant. These tables remain asynchronous best-effort mirrors owned
by `sales-dashboard`, not governed control-plane source-of-truth tables.

## Rollback-only write-contract acceptance

The exact prospective RLS/grant change ran in a transaction, followed by the
actual mirror shapes under `set local role service_role`:

- audit insert with the runtime JSON metadata envelope;
- health insert and conflict update on `source_system`;
- two-row raw import insert and conflict update on
  `(source_system, source_id, entity_type)`;
- sales invoice insert and conflict update on `stripe_invoice_id`.

Assertions proved every insert/update result. The transaction then rolled back
and returned zero audit, health, raw-import and invoice residue plus zero RLS
residue. The service role is confirmed as RLS-bypassing and retains insert/update
authority on all four tables.

## Live acceptance evidence

| Check | Result |
| --- | --- |
| Active mirror tables with RLS | 4/4 |
| Browser grants | 0 |
| Service-role insert/update table grants | 4/4 |
| Anonymous REST access across all legacy tables | 18/18 denied with HTTP 401 |
| Active mirror rows | 17 / 3 / 59 / 59, unchanged |
| No-caller table rows | 0 across all fourteen |
| Migration registry checksum | exact |
| Persistent acceptance writes | none |
| External provider writes | none |

## Independent Advisor result

A fresh Supabase Security Advisor run reports 0 errors, 15 warnings and 30
information items. This removes every RLS-disabled error from the project; it
does not promote the platform to production readiness or authorize the
remaining legacy Gift/Auth changes.
