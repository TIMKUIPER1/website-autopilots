# AP-INT-20260803-007 — Product data-plane discovery evidence

## Scope

- Migration: `20260804133000_product_data_plane_discovery.sql`
- SHA-256: `44acd756f176f5d51e7947568a8e5142dc07e8a1a2022d202a860bddba93cc61`
- Target: exact allowlisted Autopilots project `wurycoodzcybaxcgqxps`
- Input authority: approved read-only Supabase management discovery
- Risk: internal evidence only; no product access, credentials or provider writes

## Discovered truth

The current Supabase management session exposed six projects. Only three
product-related candidates were retained as bounded evidence:

- AutoPlanner candidate `ixcqwwqldptoschrbtvf`, healthy, `eu-central-1`, same
  provider organization, authority `verification_required`;
- RoofPlanner candidate `ggzapceuibzbgbevbvhx`, healthy, `eu-central-1`, same
  provider organization, authority `verification_required`;
- `AutoReviews Backups EU` candidate `zivpzcpxtqqtsrasppez`, healthy,
  `eu-central-1`, separate provider organization, authority
  `excluded_non_primary`.

`Animals Faith` and `Dakcentrale Nederland` were outside the requested product
scope and were not stored. Project-name similarity is not live data authority.

## Result and acceptance

The transaction completed with `Success. No rows returned`. Registry contract
v2 now models exactly three product data planes; Autopilots itself remains the
central control plane. Live acceptance returned:

- one verified central control plane;
- zero registered product data planes;
- three unregistered products;
- two verification candidates;
- one excluded non-primary candidate;
- unknown-profile denial HTTP 403 / SQLSTATE `42501`;
- anonymous denial HTTP 401;
- 291/291 local tests, 43 immutable migrations and 37 governed RPCs;
- Security Advisor 0 errors, 2 known warnings and 37 information suggestions;
- seven physical backups, latest 23.42 hours old, WAL-G true, PITR false and
  restore rehearsal false.

No project settings, API keys, schemas or product rows were read or changed.
No OAuth, provider account, credential, invitation, deployment or paid change
was executed.

## Next proof gate

Before either exact-name candidate can become a registered data plane, obtain
separate read-only access to its database/API surface and prove a product-owned
schema fingerprint plus runtime identity. AutoReviews needs a primary candidate;
the backup-labelled project cannot be promoted by name alone.

## Rollback

Use a separately reviewed forward migration to retire discovery evidence or
replace contract v2. Never edit the applied migration or delete audit history.
