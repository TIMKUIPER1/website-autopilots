# AP-INT-20260803-009 — Privacy-safe product snapshot contracts

## Scope

Applied `20260804143000_product_snapshot_contract_catalog.sql` to the existing
Autopilots Supabase project `wurycoodzcybaxcgqxps` under Werktoestemming A.
The immutable migration checksum is
`2dd8d5ac4ae767995ed7c0b562289cbd7388a7a3c60edf541b2b66c1cca9041a`.

This change adds a server-only, organization-scoped contract catalog for
AutoReviews, AutoPlanner and RoofPlanner. It defines exact aggregate allowlists,
900-second freshness, minimum group size five and explicit prohibitions on raw
PII, row-level records, message content, secrets, provider tokens and payment
instrument data.

## Authority boundary

- no product endpoint was called;
- no customer or row-level data was read;
- no cross-project credential was stored or shared;
- no OAuth/provider authorization was started;
- no external write or notification was enabled;
- all three contracts still require implementation and verification.

## Live evidence

- SQL transaction result: `Success. No rows returned`;
- registry contract: `autopilots.data-plane-registry.v4`;
- three snapshot contracts, zero verified, three requiring implementation;
- two verified product project identities and zero active data connections;
- direct database access, row-level data, credential sharing, provider
  authorization and external writes all `false`;
- unknown scoped callers denied with 403 and anonymous RPC calls denied with
  401;
- Security Advisor: 0 errors, 2 known warnings, 38 information suggestions;
- backup inventory: seven completed physical backups, latest age 23.74 hours,
  WAL-G enabled, PITR disabled, restore not rehearsed and production readiness
  therefore false;
- full platform gate: 299/299 tests, 45 immutable migrations and 37 governed
  RPCs.

## Rollback and next gate

No destructive rollback is authorized. Correct defects only with a new
immutable forward migration. The next step is to implement one product-owned
aggregate endpoint at a time, beginning with contract tests and small-cell
suppression. A contract may become `verified` only after separate authorization
and live read-only acceptance; activation or provider writes require additional
permission beyond Werktoestemming A.
