# AP-SEC-20260803-005 — Protect Gift GraphQL surface

- Activated: 2026-08-03
- Migration: `20260804043000_protect_gift_graphql_surface.sql`
- Supabase project: `wurycoodzcybaxcgqxps`
- Change ID: `AP-SEC-20260803-005`
- Checksum: `caa47aa8807cc804fc05f48bfde0e39de8ee43618d8045a8f5263aad64e0a1cb`
- Data, RLS policy, provider, identity, membership and external writes: not applied

## Boundary

The migration revokes all table privileges from `public`, `anon` and
`authenticated` on six `public.gift_*` tables. It preserves their RLS state,
zero-policy posture, records, schema, triggers and full service-role authority.

## Dependency evidence

- six tables, all RLS-enabled and zero policies;
- 20 rows with latest stored activity on 2026-06-22;
- no local repository caller;
- no deployed Supabase Edge Function;
- no `gift_*` PostgREST event in the available 2026-07-27 through 2026-08-03
  log window;
- browser roles could discover the objects but could not successfully access
  rows because no RLS policy existed.

This evidence supports browser-grant removal only. It does not prove that an
external service-role Gift application is retired.

## Rollback-only acceptance

The exact revokes ran inside a transaction. Assertions proved six RLS tables,
zero browser privileges inside the transaction and service-role counts of
2/2/5/10/1/0. Rollback restored all twelve anonymous/authenticated SELECT grants
and changed no data or configuration.

## Live acceptance evidence

| Check | Result |
| --- | --- |
| RLS tables | 6/6 |
| Browser grants | 0 |
| Anonymous REST access | 6/6 denied with HTTP 401 |
| Service-role table access | 6/6 |
| Service-role counts | 2 / 2 / 5 / 10 / 1 / 0 |
| Migration registry checksum | exact |
| Persistent acceptance writes | none |
| External provider writes | none |

After a fresh Supabase Security Advisor run, the project reports 0 errors, 2
warnings and 30 information items. The two warnings are the intentional bounded
session bootstrap and the project leaked-password Auth setting.
