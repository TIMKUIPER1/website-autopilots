# AP-SEC-20260803-004 — Fix legacy updated-at search path

- Activated: 2026-08-03
- Migration: `20260804040000_fix_legacy_updated_at_search_path.sql`
- Supabase project: `wurycoodzcybaxcgqxps`
- Change ID: `AP-SEC-20260803-004`
- Checksum: `1a4b2fc7994c292a927c4a1df7ae0046449e642f76b062999d736eec5e54de60`
- Data, trigger, provider, identity, membership and external writes: not applied

## Boundary

The migration sets `public.set_updated_at()` search path to `pg_catalog,
public`. It does not replace the function, change its owner/security mode, alter
its body, touch its four Gift triggers or update any Gift record.

## Read-only dependency inspection

The function is security-invoker and assigns only `new.updated_at = now()`.
Exactly four triggers depend on it: `gift_conversations`, `gift_dealerships`,
`gift_knowledge_items` and `gift_vehicle_notes`.

## Rollback-only behavior acceptance

The prospective configuration ran inside a transaction. A temporary table and
trigger executed the real function; an update changed `updated_at` from a fixed
historical value to the transaction time. The transaction rolled back with null
temporary-table residue and the original null function configuration restored.
No existing Gift row was read or written.

## Live acceptance evidence

| Check | Result |
| --- | --- |
| Function configuration | `search_path=pg_catalog, public` |
| Function body hash | recorded and unchanged by configuration-only DDL |
| Dependent triggers | 4 |
| Migration registry checksum | exact |
| Persistent acceptance writes | none |
| External provider writes | none |

After a fresh Supabase Security Advisor run, the project reports 0 errors, 14
warnings and 30 information items. The mutable-search-path finding is removed;
Gift GraphQL authority remains unchanged.
