# AP-OPS-20260803-007 — Organization operations queue

- Applied: 2026-08-03
- Project: Autopilots (`wurycoodzcybaxcgqxps`)
- Migration: `20260804080000_organization_operations_queue.sql`
- SHA-256: `34a26f185be53a84390a7b78797895d2e82b40b594b2aa8d01a73a8cc9648e5e`
- Change ID: `AP-OPS-20260803-007`
- Generic actions/automatic remediation/provider writes: disabled

## Boundary

This change replaces managed demo tasks with one stable, organization-scoped
read projection over governed tasks, active incidents, blocked onboarding steps
and failed commands. Owner, admin, operator and auditor roles may read bounded
status, priority, safe error code, summary and runbook reference across brands
in the same legal entity.

Incident private context, command payloads and command results are not exposed.
The projection cannot decide, retry, suppress, escalate or remediate anything.

## Rollback-only acceptance

The exact function ran inside a live transaction. Acceptance proved owner scope,
unknown-profile and unknown-organization denial, valid bounded evidence and no
change to commands, tasks, usage or audit rows. Rollback left four brands, zero
launch requests and removed the temporary function.

## Live acceptance

The immutable registry contains migration 30 with the exact checksum and change
ID. Live organization truth is 0 open tasks, 3 active incidents, 3 onboarding
errors, 0 failed commands and 6 source-specific signals. The current codes are:

- AutoPlanner: `AUTOPLANNER_DEPENDENCIES_MISSING` (incident and onboarding);
- AutoReviews: `AUTOREVIEWS_UNREACHABLE` incident and
  `AUTOREVIEWS_LEGACY_SOURCE_UNREACHABLE` onboarding;
- RoofPlanner: `ROOFPLANNER_API_UNREACHABLE` (incident and onboarding).

Only `service_role` can execute the RPC. Browser roles cannot, and automatic
remediation remains false. The initially duplicated change-ID metadata was
corrected transactionally from `AP-OPS-20260803-004` to
`AP-OPS-20260803-007`; the migration filename and checksum were unchanged.

## Recovery

If the view causes an incident, stop the local runtime. The source tasks,
incidents, onboarding records and commands remain untouched. Repair through a
reviewed forward migration; never fall back to demo tasks or add a generic task
executor.
