# AP-OS-20260803-013 — New software launch decisions

- Applied: 2026-08-03
- Project: Autopilots (`wurycoodzcybaxcgqxps`)
- Migration: `20260804070000_brand_launch_request_decisions.sql`
- SHA-256: `bc97606ced582003b56424aeef0e460822014bce28b1fb9b4ec9ac7342e83574`
- Change ID: `AP-OS-20260803-013`
- Brand/provider creation and writes: disabled

## Boundary

This change records an owner/admin approval or rejection for a pending R2
software-launch request. It locks the request, approval and stage command to the
current context, creates an idempotent decision command, a zero-cost usage entry
and complete audit evidence.

Approval is not application. The function cannot insert a brand, environment or
onboarding run. Database constraints keep brand creation, sandbox creation,
onboarding, provider authorization, credential storage and external writes false.
No apply function exists.

## Rollback-only acceptance

The exact function and one synthetic launch request ran inside a live transaction.
Acceptance proved owner scope, approval, exact replay, divergent-key rejection,
current-context evidence, two commands, two zero-cost usage entries and two audit
events. Rollback restored four brands, zero requests and removed the temporary
function.

## Live acceptance

The immutable registry contains migration 28 with the exact checksum and change
ID. There are exactly four brands and zero launch requests. The decision function
exists for `service_role`; `anon` and `authenticated` cannot execute it. No real
request or decision was created.

## Recovery

If the route causes an incident, stop the local runtime and preserve legitimate
decision evidence. Repair through a new reviewed forward migration. Never create
or activate a brand from approved intent without a separately authorized R3 apply
design, current owner approval and a tested recovery path.
