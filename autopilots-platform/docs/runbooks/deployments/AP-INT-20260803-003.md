# AP-INT-20260803-003 — Connector request decisions

- Applied: 2026-08-03
- Project: Autopilots (`wurycoodzcybaxcgqxps`)
- Migration: `20260804053000_connector_request_decisions.sql`
- SHA-256: `ac080880448b82dab083467dbdf6121ecd4270a9d2e4998a9a9870477eb197c3`
- Change ID: `AP-INT-20260803-003`
- Provider authorization and writes: disabled

## Boundary

An active legal-entity-wide owner/admin may approve or reject a pending
connector intent for an operating brand. The server additionally requires an
internal MFA-satisfied session and exact brand scope. This records only a human
decision; it provides no provider executor.

The RPC locks the request, original command and approval, requires the exact
current context and preserves its R2/R3 risk class. It closes the original
evidence, then creates a separate idempotent decision command, zero-cost usage
entry and audit event. Approval does not mean activation.

## Rollback-only acceptance

The exact migration and synthetic R3 request ran inside one live transaction.
It proved first approval, exact replay, divergent-key rejection, stale-context
rejection, locked evidence updates, service-role-only execution and all five
provider-effect flags false. Rollback left zero requests, workflow commands,
usage entries and decision audit events; the new function was absent afterward.

## Live acceptance

The immutable registry contains the exact filename, checksum and change ID as
migration 25. The decision RPC is executable by service role and denied to
anonymous/authenticated roles. The live connector request table still has zero
rows, so no real connector was approved or rejected. Supabase Advisor remains
at 0 errors, 2 known warnings and 31 information items.

## Recovery

If the application route misbehaves, stop the local runtime. Preserve all
decision evidence and repair through a new reviewed forward migration. Provider
systems remain untouched because no OAuth, discovery, credential or write
transport exists in this change.
