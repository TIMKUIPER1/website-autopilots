# AP-INT-20260803-002 — Connector request staging

- Applied: 2026-08-03
- Project: Autopilots (`wurycoodzcybaxcgqxps`)
- Migration: `20260804050000_connector_request_staging.sql`
- SHA-256: `bf46cc4fcd98f2c33701ae68e9a1f3a533884ab015336450723997a4f60b0342`
- Change ID: `AP-INT-20260803-002`
- Provider authorization and writes: disabled

## Boundary

This change stages internal connector intent only. It adds no OAuth callback,
provider account, discovery job, secret input or external write transport. The
database forces `provider_authorization_started`, `provider_account_connected`,
`discovery_started`, `credentials_stored` and `external_writes` to remain false.

Only an active legal-entity-wide owner/admin may stage an authorization-required
onboarding step. Each request creates an idempotent R2/R3 command, pending human
approval, zero-cost usage entry and audit event. The table and both onboarding
RPCs are service-role only; the application route additionally requires an
internal session, satisfied MFA and operating-brand membership.

## Rollback-only acceptance

The exact migration ran inside a live transaction and was rolled back. It proved:

- first request created one complete evidence bundle;
- exact replay returned the original identifiers without duplicates;
- divergent idempotency reuse and a non-authorization step failed closed;
- the Supabase step remained R3 and `approval_required`;
- browser roles had neither table nor RPC access;
- all five provider-effect flags remained false;
- workflow, usage and audit residue after rollback was zero.

## Live acceptance

The immutable registry contains the exact filename, checksum and change ID as
migration 24. All four brands return `autopilots.onboarding.v2`, six steps, one
existing product-health connection, zero connector requests and both activation
flags false. The request table has RLS enabled, browser access denied and zero
rows. No real connector request or provider operation was created.

The local managed runtime was restarted and its health contract remained green;
an anonymous POST to the new route returned HTTP 401. Supabase Advisor reports
0 errors, 2 unchanged warnings and 31 information items.

## Recovery

If the application adapter causes an incident, stop the local
`autopilots-platform` runtime. Do not drop the evidence table or rewrite the
migration. Repair database behavior through a new reviewed forward migration.
Provider systems remain untouched because this change has no provider executor.
