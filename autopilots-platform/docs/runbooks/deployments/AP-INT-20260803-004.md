# AP-INT-20260803-004 — Product-health incident consistency

- Applied: 2026-08-03
- Project: Autopilots (`wurycoodzcybaxcgqxps`)
- Migration: `20260804120000_product_health_incident_consistency.sql`
- SHA-256: `74ceaf0e0bdcbd189164398fce6c2581cd2770bcd2e4a50c6cd31d85a2f79ac5`
- Change ID: `AP-INT-20260803-004`
- Product actions, notifications and provider writes: disabled

## Boundary

Every bounded AutoReviews and AutoPlanner connector failure now maps to a
product-specific safe code with exact, versioned first-response guidance.
AutoPlanner health, readiness, access and contract failures can no longer be
stored as generic transport codes. Raw endpoint responses, private network
details and credentials remain excluded.

The health recording function now resolves a different active code for the
same product connection before opening or incrementing the current incident.
Resolved rows and health events remain available as history; no incident or
audit evidence is deleted. The current observation records how many incidents
it superseded. Replays remain idempotent.

## Live acceptance

The first SQL attempt referenced a non-existent historical `created_at` column
and was fully rolled back by the surrounding transaction. No migration record
or partial change was created. The corrected immutable migration was then
validated, checksummed and applied successfully.

The existing generic AutoPlanner `UNREACHABLE` incident was normalized to
`AUTOPLANNER_API_UNREACHABLE`; the older dependency incident was resolved as
superseded. One subsequent read-only acceptance observation reused the same
active incident and advanced its context version. Live truth after acceptance:

| Check | Result |
| --- | --- |
| Active incidents | 3 |
| Active AutoPlanner incidents | 1 |
| AutoPlanner active code | `AUTOPLANNER_API_UNREACHABLE` |
| Runbooks | 16 |
| Active incidents without runbook | 0 |
| Notification attempts / deliveries | 0 / 0 |
| Automatic remediation | disabled |
| Provider writes | disabled |
| Unknown-profile / anonymous access | denied |
| Security Advisor | 0 errors, 2 warnings, 35 info |
| Physical backups listed | 7; fresh within 36 hours |
| Restore rehearsed / production ready | false / false |

The complete local gate passed 268/268 tests before deployment. Live scope
verification passed all denial and bounded-read checks. The migration registry
contains the exact filename, checksum and change ID.

## Recovery

Stop the local `autopilots-platform` runtime to halt future monitoring writes.
Preserve health events, incidents and audit evidence. Any correction must be a
new reviewed forward migration; do not reactivate superseded incidents or
delete history. Product systems remain untouched because all probes are
read-only and no provider executor exists.
