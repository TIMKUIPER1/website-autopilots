# AP-OPS-20260803-011 — Delivery-disabled alert policy

- Applied: 2026-08-03
- Project: Autopilots (`wurycoodzcybaxcgqxps`)
- Migration: `20260804100000_alert_policy_projection.sql`
- SHA-256: `34e0d8cb9c18454abc8414beb8c593928675e1442d3f52f06f3dfe0eb1724194`
- Change ID: `AP-OPS-20260803-011`
- Notification channel: none
- External writes: disabled

## Boundary

This change registers one versioned policy for each P0–P3 severity. Policies
define minimum occurrences, escalation age, a repeat-suppression window and a
role-level escalation owner. The read model classifies each active incident as
`observe`, `human_attention` or `escalation_due`.

Repeated health observations remain one durable incident with an occurrence
counter. The projection reports how many repetitions were deduplicated; it does
not create an alert delivery record. Human review is constrained true, channel
is constrained `none`, and automatic remediation, notification delivery and
provider writes are constrained false. The server role has only `SELECT` on
the policy table.

## Live acceptance

The transaction and immutable registry insert succeeded on the exact Autopilots
project. Service-role REST returned four policies and three active incident
candidates; all three currently qualify as escalation due. Notification
attempts and deliveries are both exactly zero. Unknown-profile execution is
denied with SQLSTATE `42501`; anonymous execution returns 401.

The fresh Security Advisor reports 0 errors, 2 known warnings and 35
information items. The complete platform gate passes 246 tests, 36 immutable
migration checksums and 34 governed public RPCs.

## Recovery

Stop the local managed runtime if the projection causes an incident. Do not
edit the applied migration or interpret an advisory as a sent alert. Correct
thresholds only through a reviewed forward migration. No provider, product
service, notification channel or external record was changed.
