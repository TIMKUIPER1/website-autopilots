# AP-INT-20260803-006 — Product data-plane registry

## Scope

- Migration: `20260804130000_product_data_plane_registry.sql`
- SHA-256: `615654b9fb05f43b1037dc5bf42309fe11a35e7a7f9e8c15873ef8f1978582e6`
- Target: exact allowlisted Autopilots project `wurycoodzcybaxcgqxps`
- Risk: internal read model; no provider authorization or external writes

## Result

The transaction completed with `Success. No rows returned` and registered only
the already proven Autopilots project as the central control plane. AutoPilots,
AutoReviews, AutoPlanner and RoofPlanner remain visible as operating products;
all four product data planes are explicitly `not_registered`. No product
project reference was inferred.

The registry table is RLS-enabled, has no browser policy, grants service-role
SELECT only and permanently constrains credential material, provider
authorization and provider writes to false. The organization-scoped RPC denies
browser roles and exposes no registration command.

## Acceptance evidence

- complete platform gate: 287/287 tests;
- immutable migration inventory: 42;
- governed public RPC inventory: 37;
- owner service-role read: HTTP 200, one verified control plane, four products,
  zero registered product data planes;
- unknown profile: HTTP 403 / SQLSTATE `42501`;
- anonymous RPC: HTTP 401;
- runtime: managed sandbox, durable sessions and monitoring active, external
  writes false;
- backup evidence: seven physical backups, latest 23.31 hours old, WAL-G true;
  PITR false and restore rehearsal false, therefore production readiness false;
- freshly rerun Security Advisor: 0 errors, 2 known warnings, 36 information
  suggestions.

No OAuth flow, provider account, credential, invitation, product write,
production deployment or paid change was executed.

## Rollback

Do not edit or delete the applied migration. If the read model must be removed,
ship a separately reviewed forward migration that revokes the RPC and retires
the registration without deleting history. Stop the local runtime to halt
future monitoring writes; product systems remain untouched.
