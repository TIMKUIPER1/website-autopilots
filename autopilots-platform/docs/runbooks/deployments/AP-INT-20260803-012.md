# AP-INT-20260803-012 — Atomic product snapshot evidence

## Status

Live applied and independently accepted on 2026-08-03 behind
`AP-INT-20260803-010` and `AP-INT-20260803-011`.

## Scope

- Migration: `20260804160000_atomic_product_snapshot_evidence.sql`
- SHA-256: `199e05eb4fcb58a0c344f4546cb7c1fde976c164b99ea9f0dfac320d9d83ec12`
- Target: existing Autopilots control plane `wurycoodzcybaxcgqxps`
- Risk: atomic R1 internal evidence orchestration; no product/provider write

The RPC records exactly `contract_probe`, `privacy_probe` and `freshness_probe`
from one observation. Each record remains a complete idempotent R1 command with
audit and usage evidence. PostgreSQL transaction semantics roll back the entire
RPC when any of the three records fails.

## Local acceptance evidence

- 48 immutable migrations and 40 governed RPCs validate;
- the full platform gate passes 372 tests;
- canonical hashing is deterministic across object key order and domain
  separated per gate;
- stale, privacy-unsafe and forged snapshots fail before persistence;
- the managed route requires internal scope plus MFA and accepts no browser
  evidence payload;
- repository output validation rejects partial batches or activation claims;
- data connection, provider authorization and external writes remain false.

## Live acceptance procedure

1. Apply the exact three-change chain from `PRODUCT_READINESS_CHAIN.md`; it
   records this migration as `AP-INT-20260803-012` without exposing a partial
   intermediate state.
2. Confirm live inventory is exactly 48 migrations and 40 governed RPCs.
3. In the governed rolled-back acceptance, submit a valid synthetic snapshot batch and
   prove three evidence rows, three commands, three usage rows and three audit
   events share one observation.
4. Force the third record to fail and prove zero rows from the full batch remain.
5. Verify exact replay, divergent replay, cross-organization, auditor, anonymous
   and authenticated-browser denials.
6. Require the independent postcheck to prove zero synthetic residue, then
   rerun Security Advisor and backup inventory.

Do not use real product secrets or expose a verification UI during migration
acceptance. A successful batch is technical evidence only, never activation.

All listed steps passed inside rollback-only acceptance. The valid batch wrote
three temporary evidence/command/usage/audit records, the forced third-record
failure left no partial batch, all scope/role/replay denials held and the final
independent verifier proved zero synthetic residue.

## Rollback

Before application, remove only through reviewed source history. After live
application, preserve command/audit/evidence history and correct forward; do not
delete recorded proof or weaken atomicity.
