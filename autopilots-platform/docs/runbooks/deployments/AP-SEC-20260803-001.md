# AP-SEC-20260803-001 — Live negative scope acceptance

- Verified: 2026-08-03
- Project: Autopilots (`wurycoodzcybaxcgqxps`)
- Contract: `autopilots.live-scope-denial.v1`
- Persistent writes: none
- External/provider writes: none

## Result

The credential-safe verifier executed nine read-only negative authorization
checks against the exact live Supabase project. Wrong legal-entity scope,
unknown profile and anonymous calls all failed closed.

| Boundary | Result |
| --- | --- |
| Owner → unrelated portfolio identifier | HTTP 403 / `42501` |
| Unknown profile → real portfolio | HTTP 403 / `42501` |
| Unknown profile → real brand twin | HTTP 403 / `42501` |
| Unknown profile → real onboarding | HTTP 403 / `42501` |
| Unknown profile → real incidents | HTTP 403 / `42501` |
| Owner → unknown access-roster organization | denied / `P0002` |
| Anonymous → portfolio | HTTP 401 / `42501` |
| Anonymous → brand twin | HTTP 401 / `42501` |
| Anonymous → access roster | HTTP 401 / `42501` |

This acceptance proves negative handling for invalid identifiers and authority.
It is not evidence of isolation between two existing tenants. The rollback-only
two-tenant procedure in `docs/runbooks/TENANT_ISOLATION.md` remains required
before production cutover.
