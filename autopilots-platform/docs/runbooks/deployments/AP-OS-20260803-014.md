# AP-OS-20260803-014 — Organization approval queue

- Applied: 2026-08-03
- Project: Autopilots (`wurycoodzcybaxcgqxps`)
- Migration: `20260804073000_organization_approval_queue.sql`
- SHA-256: `4a49b6d15eafe0ab40d256196efb7d128eb2568d160f375d0ec6d091c90e88a1`
- Change ID: `AP-OS-20260803-014`
- Generic decisions/provider writes: disabled

## Boundary

This change replaces managed demo approvals with a stable organization-scoped
read projection over durable workflow approvals. Owner, admin and auditor roles
may read bounded status, risk, context, rationale and evidence for brands in the
same legal entity. Command payloads and results are not exposed.

The projection cannot decide or execute an approval. Generic decisions, provider
authorization and external writes are explicitly false; typed workflows retain
their own MFA and current-context requirements.

## Rollback-only acceptance

The exact function ran inside a live transaction. Acceptance proved owner scope,
unknown-profile and unknown-organization denial, valid no-effect contract and no
change to commands, usage or audit rows. Rollback left four brands, zero launch
requests and removed the temporary function.

## Live acceptance

The immutable registry contains migration 29 with the exact checksum and change
ID. The queue returns one historical approval and zero pending approvals. Four
brands and zero launch requests remain. Only `service_role` can execute the RPC;
`anon` and `authenticated` cannot.

## Recovery

If the view causes an incident, stop the local runtime. The underlying workflow
evidence remains untouched. Repair the projection through a reviewed forward
migration; never fall back to demo approvals or add a generic decision endpoint.
