# AP-OPS-20260803-010 — Brand-scoped error runbooks

- Applied: 2026-08-03
- Project: Autopilots (`wurycoodzcybaxcgqxps`)
- Migration: `20260804093000_error_runbook_catalog.sql`
- SHA-256: `cfa1ec688fc50baf49c8fba18f59216ff08dd14ca1876af20729cf375dc7b4ae`
- Change ID: `AP-OPS-20260803-010`
- External writes: disabled

## Boundary

This change adds a durable error-runbook catalog keyed by exact operating brand
and stable error code. Seven versioned runbooks cover the current AutoReviews,
AutoPlanner and RoofPlanner reachability, destination and dependency signals.
Each record contains bounded first-response guidance and a named escalation
owner.

The catalog is guidance only. Database checks keep automatic remediation,
notification delivery and provider writes false. The service role has `SELECT`
only and all mutation privileges are explicitly revoked. Browser roles have no
table or RPC access. Unmatched codes remain visible but fail closed to human
triage instead of receiving generic executable advice.

## Live acceptance

The first editor attempt encountered hidden stale content and was fully rolled
back by the transaction after the immutable migration registry rejected a
duplicate old version. The active editor was cleared, verified and rerun; the
catalog transaction then succeeded with change ID `AP-OPS-20260803-010`.

Service-role REST returns seven runbooks. Unknown-profile execution is denied
with SQLSTATE `42501` and anonymous execution with 401. All three no-effect
flags are false. The fresh Security Advisor reports 0 errors, 2 known warnings
and 34 information items.

The complete platform gate passes 239 tests, 35 immutable migration checksums
and 33 governed public RPCs.

## Recovery

Stop the local managed runtime if the UI projection causes an incident. Do not
edit the applied migration or delete catalog rows. Correct guidance through a
reviewed forward migration; no provider, product-owned data or external channel
was changed.
