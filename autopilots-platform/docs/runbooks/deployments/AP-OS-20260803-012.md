# AP-OS-20260803-012 — New software launch request staging

- Applied: 2026-08-03
- Project: Autopilots (`wurycoodzcybaxcgqxps`)
- Migration: `20260804063000_brand_launch_request_staging.sql`
- SHA-256: `2d5ecf781831e93800e161e3ffd4b3b83d5a45cc8c7cc167de7b2507e291396e`
- Change ID: `AP-OS-20260803-012`
- Brand/provider creation and writes: disabled

## Boundary

This change stages a proposed software brand under the existing Autopilots
operating brand and sandbox authority. It validates a unique slug, legal-entity
code, bounded display name and risk profile. It creates an R2 command, pending
approval, zero-cost usage entry and audit event only.

Database constraints permanently keep `brand_created`,
`sandbox_environment_created`, `onboarding_run_created`,
`provider_authorization_started`, `credentials_stored` and `external_writes`
false. No apply function exists.

## Rollback-only acceptance

The exact migration and one synthetic launch request ran inside a live
transaction. Acceptance proved Autopilots authority, first request, exact replay,
divergent-key rejection, existing-slug rejection, complete evidence and
service-role-only access. Rollback restored four brands and left zero workflow,
usage, audit, table or function residue.

## Live acceptance

The immutable registry contains migration 27 with the exact checksum and change
ID. There are still exactly four operating brands and zero launch requests. The
owner registry returns `canManageBrandLaunches=true`, while brand creation,
provider authorization and external writes are false. Anonymous staging is
denied.

The portfolio UI presents a bounded intake form and explicitly says that no
brand, sandbox, onboarding run, provider account or credential is created.

## Recovery

If the intake route causes an incident, stop the local runtime. Preserve any
legitimate staged intent and repair through a new reviewed forward migration.
Never turn an approved request into a brand without a separately reviewed R3
apply design and explicit authority.
