# AP-INT-20260803-011 — Product connection evidence recording

## Status

Locally verified; live application pending behind `AP-INT-20260803-010`. No
database statement from this change has been executed against Autopilots.

## Scope

- Migration: `20260804153000_product_connection_evidence_recording.sql`
- SHA-256: `d2b557c13aedab1f3dd5604de6cf21e9de6473dbc611f7a65d41ee6ff78f7b8c`
- Target: existing Autopilots control plane `wurycoodzcybaxcgqxps`
- Risk: R1 internal evidence recording; no product, provider or external effect

The change makes product-connection evidence append-only and adds one
service-role-only recorder. It accepts only a SHA-256, bounded source category,
gate key, pass/fail result and fresh timestamp. It derives expiry from policy,
creates one idempotent command, audit event and zero-cost usage row, and cannot
record project identity or current human approval.

## Local acceptance evidence

- 48 immutable local migrations and 40 governed RPCs validate across the full
  pending chain;
- the full platform gate passes 355 tests;
- direct browser execution is denied;
- stale/future observations and cross-source gate claims are rejected;
- raw evidence, endpoints, credentials, tokens and activation flags cannot be
  supplied;
- data connection, provider authorization and external writes remain false.

## Pending live procedure

1. Apply the exact three-change chain from `PRODUCT_READINESS_CHAIN.md`; it
   preserves this migration's change ID `AP-INT-20260803-011` while preventing
   a partial intermediate state.
2. Require the independent post-apply inventory to be exactly 48 migrations
   and 40 governed RPCs.
3. In the governed rolled-back acceptance, verify owner/operator success, auditor and
   cross-organization denial, exact replay, divergent replay rejection, stale
   rejection, source ownership and update/delete denial.
4. Confirm the independent postcheck proves zero evidence, command, usage and
   audit residue.
5. Rerun the Security Advisor and record the resulting live 48/40 inventory.

Do not record real gate evidence, connect product endpoints or enable any
provider while this chain is being accepted.

## Rollback

Before live application, discard only through reviewed source history. After
application, preserve append-only evidence and correct forward; do not drop the
function or mutate/delete recorded proof.
