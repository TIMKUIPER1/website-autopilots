# AP-INT-20260803-010 — Product connection readiness

## Status

Locally verified; live application pending. No database statement from this
change has been executed against the Autopilots project.

## Scope

- Migration: `20260804150000_product_connection_readiness.sql`
- Target: existing Autopilots control plane `wurycoodzcybaxcgqxps`
- Risk: internal read model only; no connector, credential, product or provider
  write authority
- Contract: `autopilots.product-connection-readiness.v1`

The migration adds a twelve-gate policy catalog, an immutable evidence table
that stores hashes rather than evidence payloads, and one organization-scoped
service-role read function. Direct runtime writes to both tables are denied.
The current human-approval gate is deliberately always blocked, so this version
cannot claim activation readiness even if technical evidence is later present.

## Local acceptance evidence

- 46 immutable local migrations and 38 governed RPCs validate;
- the full platform gate passes 339 tests across the current three-migration
  readiness chain;
- the repository rejects missing, reordered or invented gates;
- stale and overlong evidence fails closed;
- forged readiness, data-connection, provider-authorization and external-write
  claims are rejected;
- the HTTP route requires a managed internal organization session.

## Pending live procedure

1. Restore approved non-interactive access to the linked Supabase project.
2. Run the default read-only preflight from `PRODUCT_READINESS_CHAIN.md`; it
   must confirm the exact ordered 45-file checksum inventory.
3. Use the gated chain runner to apply this migration and its two reviewed
   dependents in one transaction. This registry entry retains change ID
   `AP-INT-20260803-010`.
4. Verify three products, twelve gates per product, zero ready products, zero
   evidence rows and all three external-effect flags false.
5. Verify anonymous and authenticated browser roles are denied and rerun the
   Supabase security advisor.
6. Continue the same transaction acceptance with dependent changes
   `AP-INT-20260803-011` and `AP-INT-20260803-012`.

The official linked CLI currently fails before query execution while creating
its temporary database login. The alternative Management API path requires a
macOS Keychain release that was not granted. Do not bypass either control with
copied credentials, raw secrets or an unregistered SQL Editor change.

## Rollback

Before live application, discard the pending migration only through reviewed
source history. After live application, preserve policy and evidence history
and correct forward; do not drop tables, rewrite evidence or remove auditability.
