# AP-INT-20260803-008 — Verified product data-plane identities

## Scope

- Migration: `20260804140000_verified_product_data_planes.sql`
- SHA-256: `a14023a7507f4cd9985f8b63b747ebe85d9f0fb11873b0d5910d5ffb0303a8a3`
- Target: exact allowlisted Autopilots project `wurycoodzcybaxcgqxps`
- Evidence: exact-target HTTPS GET of OpenAPI metadata; no table rows read
- Risk: internal identity registration; no product data authorization or writes

## Schema evidence

The existing management session supplied transient project credentials only in
memory. They were sent only to their matching allowlisted Supabase origins,
were not printed or stored and were used solely for `GET /rest/v1/` OpenAPI
metadata.

- AutoPlanner `ixcqwwqldptoschrbtvf`: 54 paths, canonical newline-sorted
  SHA-256 `bc1b8190d9759a4d393974fb5c4dcd27c6568b358c4b59207bdd717ce09c3704`.
  The schema contains vehicles/leads, appointments, trade-ins and bounded
  calendar/provider automation contracts.
- RoofPlanner `ggzapceuibzbgbevbvhx`: 38 paths, canonical newline-sorted
  SHA-256 `778ce86830ba4f140906ae30c8cd6e7963d12614e76cef09b5bb81c1703d336a`.
  The schema contains explicit RoofPlanner trial bootstrap, organizations,
  employees, calendars, provider operations and usage evidence.
- `AutoReviews Backups EU` `zivpzcpxtqqtsrasppez`: 0 paths, empty-set SHA-256
  `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.
  It remains excluded as non-primary and is not registered as a data plane.

## Execution safety

The first SQL-editor attempt was cancelled at a broad-update warning. A clean
read-only inspection proved 0 new columns, 0 verified product rows and 0
migration rows. The migration was then bounded with an explicit three-project
`WHERE` clause, rehashed and executed once from a clean SQL tab. It completed
with `Success. No rows returned`.

## Live acceptance

- registry contract `autopilots.data-plane-registry.v3`;
- three registered projects: one control plane plus two product identities;
- AutoPlanner and RoofPlanner project references match the schema evidence;
- two verified product identities, zero active data connections;
- AutoReviews remains `not_registered`; its backup candidate remains excluded;
- provider authorization, cross-project credential sharing and external writes
  are false;
- unknown profile denied HTTP 403 / SQLSTATE `42501`; anonymous denied HTTP 401;
- 295/295 tests, 44 immutable migrations and 37 governed RPCs;
- Security Advisor: 0 errors, 2 known warnings, 37 information suggestions;
- seven physical backups, latest 23.59 hours old, WAL-G true, PITR false and
  restore rehearsal false.

## Next gate

A verified identity is not a live data connection. Each product needs a
separately authorized read-only connector, scoped vault reference, explicit
dataset contract, reconciliation policy, rate limits and revocation runbook.
AutoReviews first needs a proven primary project.

## Rollback

Use a new reviewed forward migration to pause or retire a product registration.
Do not delete fingerprints, edit applied migrations or rotate product keys as
part of rollback.
