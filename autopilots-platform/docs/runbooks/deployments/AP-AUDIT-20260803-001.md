# AP-AUDIT-20260803-001 — Organization audit timeline

- Applied: 2026-08-03
- Project: Autopilots (`wurycoodzcybaxcgqxps`)
- Migration: `20260804110000_organization_audit_timeline.sql`
- SHA-256: `7b368b2f6b0c2557ea3e48a231aa3752946b8839097981279ae151cff167af36`
- Change ID: `AP-AUDIT-20260803-001`
- External writes: disabled

## Boundary

This change adds one organization-scoped read model over the existing
append-only audit ledger. Only legal-entity-wide owners, admins and auditors
may request it through the server role. The projection returns the latest 50
events plus bounded 24-hour totals.

Action, actor category, safe actor label, result, risk class, source, operating
brand, time and correlation ID are visible. Raw actor IDs, reasons,
before/after values and evidence payloads are excluded. Browser roles are
revoked and the UI exposes no replay, mutation or provider action.

## Live acceptance

The exact migration and registry row were applied transactionally to the
authorized project. Service-role verification returned 50 recent events and
117 events in the latest 24 hours, with 0 failed and 0 blocked. Unknown-profile
execution is denied with SQLSTATE `42501`; anonymous execution returns 401.
Private payload exposure, generic actions and external writes are false.

The freshly rerun Security Advisor reports 0 errors, 2 known warnings and 35
information items. The complete platform gate passes 259 tests, 38 immutable
migration checksums and 36 governed public RPCs. Seven physical backups remain
fresh with WAL-G, while PITR and restore rehearsal remain absent; production
readiness is therefore still false.

## Recovery

Stop the managed runtime if the projection causes an incident. Revoke server
execution through a reviewed forward migration if necessary; never edit this
applied migration or mutate the append-only ledger.
