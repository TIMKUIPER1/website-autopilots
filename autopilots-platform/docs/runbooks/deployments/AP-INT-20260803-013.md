# AP-INT-20260803-013 — Provider-neutral product runtime topology

- Change ID: `AP-INT-20260803-013`
- Status: locally verified; live application pending after the exact 48-migration readiness posture
- Migration: `20260804163000_product_runtime_topology.sql`
- SHA-256: `1b4887e72abce6dcd76be5e331d31691747eff57dcc4c9ca764a7c74db4062c1`
- Target: existing Autopilots control plane `wurycoodzcybaxcgqxps`
- Risk: R0 internal read model; no endpoint, credential, provider or product write authority

## Purpose

The Supabase project registry is intentionally provider-specific and must not
pretend that every product runs on Supabase. This additive read model records
one bounded primary runtime identity per product:

- AutoReviews: Render managed service with persistent SQLite, verified from the
  product repository architecture. Its separate Supabase backup project stays
  excluded from operational data-plane authority.
- AutoPlanner and RoofPlanner: Supabase projects with PostgreSQL, linked only to
  their already verified product-data-plane identities.

No host name, endpoint, service identifier, project credential or provider
token is stored. Every row constrains endpoint verification, credentials, data
connection, provider authorization and external writes to false.

## Local acceptance

- The table has RLS, no browser grants and service-role SELECT only.
- The organization-scoped RPC is service-role only and exposes three bounded
  identities without provider resource references.
- The repository joins runtime identity to the existing Supabase topology and
  rejects missing, duplicate or cross-product runtime sets.
- The portfolio presents `Render · SQLite` separately from
  `Supabase · PostgreSQL` and keeps active data connections at zero.
- The complete platform gate passes with 49 migrations, 41 governed RPCs and
  all external effects disabled.

## Live procedure

1. Complete and independently accept the exact 45→48 readiness chain.
2. Require live inventory to match all 48 filenames and checksums.
3. Apply only this migration using the project-pinned 48→49 runner and change
   ID above.
4. Run the separate SELECT-only verifier: exactly three runtime identities,
   one Render/SQLite and two Supabase/PostgreSQL; RLS and role denials must pass.
5. Confirm 49 migrations, 41 governed RPCs, zero active data connections and
   no credential, provider-authorization or external-write state.

The combined Keychain-backed foundation release executes these stages in this
order and stops on the first failed proof. It never prints or persists the
Supabase access token.

## Rollback

Before application, remove only through reviewed source history. After live
application, correct forward. Never relabel the AutoReviews backup project as
an operational backend and never weaken the no-effect constraints.
